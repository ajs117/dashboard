# Keep a small, capped persistent journal so a crash can be diagnosed after a reboot
# (default here was volatile = logs lost on reboot). Capped to limit SD-card writes.
# Installed to /etc/systemd/journald.conf.d/.
[Journal]
Storage=persistent
SystemMaxUse=60M
SystemMaxFileSize=10M
